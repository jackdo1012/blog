<style>
    .category__title {
        font-size: 2.5rem;
        font-weight: bold;
        text-align: center;
        padding: 1rem;
        color: var(--dark-highlight-text);
    }
    .category__blog-list {
        display: flex;
        flex-direction: column;
        padding: 1rem;
    }
    .category__blog-list--item {
        padding: 1rem;
    }
    .item__title {
        font-size: 1.5rem;
        font-weight: bold;
        color: var(--dark-highlight-text);
        padding: 0.5rem 0;
    }
    .item__title:hover {
        color: var(--dark-highlight-hover) !important;
    }
    .item__title:visited {
        color: var(--dark-highlight-text);
    }
    .item__description {
        color: var(--dark-main-text);
        padding: 0.5rem 0;
    }
    .item__date {
        color: var(--dark-main-text);
        opacity: 0.7;
    }
</style>

