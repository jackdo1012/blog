<style>
    .about {
        width: var(--page-width);
        margin-left: var(--nav-width);
        background-color: var(--dark-page-bg);
        height: 100vh;
    }
    .about__title {
        font-size: 2.5rem;
        text-align: center; color: var(--dark-highlight-text);
        padding: 1rem;
        font-weight: bold;
    }
    .about__text {
        font-size: 1.25rem;
        text-align: center;
        color: var(--dark-main-text);
        padding: 1rem;
    }
    .social-list {
        color: var(--dark-main-text);
        display: flex;
        justify-content: center;
        align-items: center;
        flex-direction: row;
        list-style: none;
    }
    .social-list a {
        color: var(--dark-main-text);
    }
    .social-list a:visited {
        color: var(--dark-main-text);
    }

    .social-list a i {
        font-size: 2rem;
        margin: 0.5rem;
    }
</style>
